import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "DouingIt - Preserve Your Family's Legacy",
  description: "Your legacy carries a wisdom and strength that needs nurturing. We offer simple, guided pathways to preserve traditions, tell family stories, and build stronger inter-generational bonds that last.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}